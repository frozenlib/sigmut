name: Rust

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        features:
          - --all-features
          - --features smol
          - --features tokio
          - --features async-std
          -
    steps:
      - uses: actions/checkout@v2

      - name: Cache cargo registry
        uses: actions/cache@v1
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}-1
          restore-keys: ${{ runner.os }}-cargo-registry

      - name: Cache cargo index
        uses: actions/cache@v1
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}-1
          restore-keys: ${{ runner.os }}-cargo-index

      - name: Cache cargo build
        uses: actions/cache@v1
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}-1
          restore-keys: ${{ runner.os }}-cargo-build-target

      - name: Build
        run: cargo build --verbose ${{ matrix.features }}

      - name: Run tests no run
        if: ${{ matrix.features != '--all-features' }}
        run: cargo test --verbose ${{ matrix.features }} --no-run

      - name: Run tests
        if: ${{ matrix.features == '--all-features' }}
        run: cargo test --verbose ${{ matrix.features }}
